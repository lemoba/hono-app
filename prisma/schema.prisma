generator client {
  provider        = "prisma-client-js"
  output          = "../src/models/client"
  previewFeatures = ["deno"]
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_admin_menus {
  id         Int       @id @default(autoincrement()) @db.UnsignedInt
  parent_id  Int       @default(0) @db.UnsignedInt
  path       String    @default("")
  name       String    @default("")
  type_name  String    @default("")
  component  String    @default("")
  sort       Int       @default(0)
  icon       String    @default("")
  created_at DateTime  @default(now()) @db.Timestamp(0)
  updated_at DateTime? @default(now()) @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_admin_roles {
  id         Int       @id @default(autoincrement()) @db.UnsignedInt
  name       String    @default("")
  status     Boolean?  @default(true)
  desc       String    @default("")
  rules      Json?
  created_at DateTime  @default(now()) @db.Timestamp(0)
  updated_at DateTime? @default(now()) @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_admin_tokens {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  user_id    Int       @db.UnsignedInt
  token      String?   @db.Text
  created_at DateTime? @default(now()) @db.Timestamp(0)
  updated_at DateTime? @default(now()) @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)

  @@index([user_id], map: "idx_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_admins {
  id              Int       @id @default(autoincrement()) @db.UnsignedInt
  username        String    @default("") @db.VarChar(50)
  password        String    @db.VarChar(255)
  password_modify Int       @default(0) @db.UnsignedTinyInt
  roles           Json?
  created_at      DateTime  @default(now()) @db.Timestamp(0)
  updated_at      DateTime? @default(now()) @db.Timestamp(0)
  deleted_at      DateTime? @db.Timestamp(0)
  status          Boolean   @default(true)
}

model sp_failed_jobs {
  id         BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  uuid       String   @unique(map: "sp_failed_jobs_uuid_unique") @db.VarChar(255)
  connection String   @db.Text
  queue      String   @db.Text
  payload    String   @db.LongText
  exception  String   @db.LongText
  failed_at  DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_files {
  id         Int       @id @default(autoincrement()) @db.UnsignedInt
  file_id    String    @unique(map: "idx_fid") @default("0") @db.VarChar(50)
  url        String    @default("") @db.VarChar(255)
  title      String    @default("") @db.VarChar(255)
  type       Int       @default(1) @db.UnsignedTinyInt
  group_id   Int       @default(0)
  sort       Int?      @default(0)
  cover      String?   @default("") @db.VarChar(255)
  info       Json?
  created_at DateTime? @default(now()) @db.Timestamp(0)
  updated_at DateTime? @default(now()) @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)

  @@index([group_id], map: "idx_group_id")
  @@fulltext([title], map: "idx_title")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_goods {
  id           BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  type         Int       @default(1) @db.UnsignedTinyInt
  name         String    @default("") @db.VarChar(255)
  price        Decimal   @default(0.00) @db.Decimal(10, 2)
  coins        Int       @default(0)
  product_tier String?   @default("") @db.VarChar(255)
  bonus        Int       @default(0)
  sort         Int?      @default(0)
  desc         String    @default("") @db.VarChar(255)
  created_at   DateTime  @default(now()) @db.Timestamp(0)
  updated_at   DateTime? @default(now()) @db.Timestamp(0)
  deleted_at   DateTime? @db.Timestamp(0)

  @@index([type], map: "idx_type")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_home_series {
  id         Int       @id @default(autoincrement()) @db.UnsignedInt
  title      String    @default("") @db.VarChar(255)
  icon       String    @default("") @db.VarChar(255)
  desc       String    @default("") @db.VarChar(255)
  sort       Int       @default(0)
  is_more    Boolean?  @default(false)
  created_at DateTime? @default(now()) @db.Timestamp(0)
  updated_at DateTime? @default(now()) @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_login_logs {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  user_id    BigInt    @db.UnsignedBigInt
  logs       Json?
  created_at DateTime? @default(now()) @db.Timestamp(0)
  updated_at DateTime? @default(now()) @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)

  @@index([user_id], map: "idx_user_id")
}

model sp_migrations {
  id        Int    @id @default(autoincrement()) @db.UnsignedInt
  migration String @db.VarChar(255)
  batch     Int
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_orders {
  id              BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  order_id        String    @default("") @db.VarChar(100)
  pay_order_id    String?   @default("") @db.VarChar(100)
  user_id         BigInt
  goods_id        Int
  total_amount    Decimal   @db.Decimal(10, 2)
  actual_amount   Decimal   @default(0.00) @db.Decimal(10, 2)
  pay_type        Boolean
  callback_status Boolean?  @default(false)
  status          Boolean   @default(true)
  os_type         Boolean?  @default(true)
  dev_no          String    @default("") @db.VarChar(255)
  order_ip        String    @default("") @db.VarChar(60)
  order_country   String?   @default("") @db.VarChar(60)
  created_at      DateTime  @default(now()) @db.Timestamp(0)
  updated_at      DateTime? @default(now()) @db.Timestamp(0)
  deleted_at      DateTime? @db.Timestamp(0)

  @@index([order_id], map: "idx_order_id")
  @@index([status], map: "idx_status")
  @@index([user_id], map: "idx_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_payment_records {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  order_id   String    @default("") @db.VarChar(100)
  user_id    BigInt
  amount     Decimal?  @default(0.00) @db.Decimal(10, 2)
  coins      Int?      @default(0) @db.UnsignedInt
  bonus      Int?      @default(0) @db.UnsignedInt
  type       Boolean?  @default(true)
  created_at DateTime  @default(now()) @db.Timestamp(0)
  updated_at DateTime? @default(now()) @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)

  @@index([order_id], map: "idx_order_id")
  @@index([user_id], map: "idx_uid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_privacy_settings {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  title      String?   @db.VarChar(255)
  type       Int       @default(1) @db.UnsignedTinyInt
  content    String?   @db.Text
  created_at DateTime  @default(now()) @db.Timestamp(0)
  updated_at DateTime? @default(now()) @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_purchase_records {
  id             BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  user_id        BigInt
  video_group_id Int
  video_id       Int
  costs          Int?      @default(0)
  pay_type       Int?      @default(1) @db.TinyInt
  created_at     DateTime  @default(now()) @db.Timestamp(0)
  updated_at     DateTime? @default(now()) @db.Timestamp(0)
  deleted_at     DateTime? @db.Timestamp(0)

  @@index([user_id, video_group_id], map: "idx_uid_group_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_reward_records {
  id         Int       @id @default(autoincrement()) @db.UnsignedInt
  user_id    BigInt    @db.UnsignedBigInt
  reward_id  Int       @db.UnsignedInt
  updated_at DateTime? @db.DateTime(0)
  created_at DateTime  @default(now()) @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)

  @@index([user_id], map: "idx_uid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_rewards_list {
  id         Int       @id @default(autoincrement()) @db.UnsignedInt
  type       Boolean
  is_once    Boolean   @default(false)
  max_nums   Int       @default(0) @db.UnsignedTinyInt
  sort       Int?      @default(0) @db.MediumInt
  bonus      Int       @default(0) @db.UnsignedInt
  updated_at DateTime? @db.DateTime(0)
  created_at DateTime  @default(now()) @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_rewards_type {
  id         Int       @id @default(autoincrement()) @db.UnsignedInt
  name       String    @default("") @db.VarChar(255)
  status     Boolean   @default(true)
  updated_at DateTime? @db.DateTime(0)
  created_at DateTime  @default(now()) @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_tokens {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  user_id    BigInt    @db.UnsignedBigInt
  token      String?   @db.Text
  created_at DateTime? @default(now()) @db.Timestamp(0)
  updated_at DateTime? @default(now()) @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)

  @@index([user_id], map: "idx_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_upload_logs {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  user_id    BigInt
  sid        BigInt
  name       String    @db.VarChar(255)
  file_id    String    @default("") @db.VarChar(255)
  created_at DateTime  @default(now()) @db.Timestamp(0)
  updated_at DateTime? @default(now()) @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)

  @@index([user_id, sid], map: "idx_uid_sid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_user_check_in {
  id         Int       @id @default(autoincrement()) @db.UnsignedInt
  user_id    BigInt    @db.UnsignedBigInt
  bonus      Int?      @default(0) @db.UnsignedInt
  days       Int       @default(1) @db.UnsignedInt
  type       Boolean?  @default(true)
  updated_at DateTime? @db.DateTime(0)
  created_at DateTime  @default(now()) @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)

  @@index([created_at], map: "idx_created_at")
  @@index([user_id], map: "idx_uid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_user_collections {
  id             Int       @id @default(autoincrement()) @db.UnsignedInt
  user_id        BigInt
  video_id       BigInt
  video_group_id BigInt
  status         Boolean
  created_at     DateTime  @default(now()) @db.Timestamp(0)
  updated_at     DateTime? @default(now()) @db.Timestamp(0)
  deleted_at     DateTime? @db.Timestamp(0)

  @@index([user_id], map: "idx_uid")
  @@index([video_group_id], map: "idx_video_group_id")
  @@index([video_id], map: "idx_video_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_user_likes {
  id             Int       @id @default(autoincrement()) @db.UnsignedInt
  user_id        BigInt
  video_id       BigInt
  video_group_id BigInt
  status         Boolean
  created_at     DateTime  @default(now()) @db.Timestamp(0)
  updated_at     DateTime? @default(now()) @db.Timestamp(0)
  deleted_at     DateTime? @db.Timestamp(0)

  @@index([user_id], map: "idx_uid")
  @@index([video_group_id], map: "idx_video_group_id")
  @@index([video_id], map: "idx_video_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_user_my_lists {
  id             BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  user_id        BigInt    @db.UnsignedBigInt
  video_group_id Int       @db.UnsignedInt
  created_at     DateTime? @default(now()) @db.Timestamp(0)
  updated_at     DateTime? @default(now()) @db.Timestamp(0)
  deleted_at     DateTime? @db.Timestamp(0)

  @@index([user_id, video_group_id], map: "idx_uid_vgid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_user_suggests {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  user_id    BigInt
  title      String    @default("") @db.VarChar(255)
  content    String    @db.Text
  created_at DateTime  @default(now()) @db.Timestamp(0)
  updated_at DateTime? @default(now()) @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)
  email      String?   @db.VarChar(255)

  @@index([user_id], map: "idx_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_users {
  id               BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  user_id          BigInt    @db.UnsignedBigInt
  open_id          String?   @default("") @db.VarChar(255)
  bonus            Int?      @default(0) @db.UnsignedInt
  coins            Int       @default(0) @db.UnsignedInt
  email            String?   @default("") @db.VarChar(50)
  avatar           String?   @default("") @db.VarChar(255)
  nickname         String?   @default("") @db.VarChar(50)
  age              Int       @default(0) @db.UnsignedTinyInt
  gender           Int       @default(0) @db.UnsignedTinyInt
  login_type       Int       @default(1) @db.UnsignedTinyInt
  last_login_time  DateTime? @db.DateTime(0)
  register_ip      String?   @default("") @db.VarChar(50)
  login_ip         String?   @default("") @db.VarChar(50)
  register_country String?   @default("") @db.VarChar(60)
  login_country    String?   @default("") @db.VarChar(60)
  register_dev_no  String?   @default("") @db.VarChar(100)
  login_dev_no     String?   @default("") @db.VarChar(100)
  register_os_type Boolean?  @default(true)
  login_os_type    Boolean?  @default(true)
  is_disable       Int       @default(1) @db.UnsignedTinyInt
  created_at       DateTime? @default(now()) @db.Timestamp(0)
  updated_at       DateTime? @default(now()) @db.Timestamp(0)
  deleted_at       DateTime? @db.Timestamp(0)

  @@index([is_disable], map: "idx_is_disable")
  @@index([open_id(length: 20)], map: "idx_open_id")
  @@index([user_id], map: "idx_user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_video_categories {
  id         Int       @id @default(autoincrement()) @db.UnsignedInt
  name       String    @default("") @db.VarChar(255)
  desc       String    @default("") @db.VarChar(255)
  status     Boolean?  @default(true)
  created_at DateTime? @default(now()) @db.Timestamp(0)
  updated_at DateTime? @default(now()) @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)

  @@fulltext([name], map: "name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_video_group_categories {
  id             BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  video_group_id Int       @db.UnsignedInt
  category_id    Int       @db.UnsignedInt
  created_at     DateTime? @default(now()) @db.Timestamp(0)
  updated_at     DateTime? @default(now()) @db.Timestamp(0)
  deleted_at     DateTime? @db.Timestamp(0)

  @@index([video_group_id, category_id], map: "idx_vgid_cid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_video_groups {
  id           Int       @id @default(autoincrement()) @db.UnsignedInt
  series_id    Int       @default(0) @db.UnsignedInt
  category_id  Int?      @default(0)
  title        String    @default("") @db.VarChar(255)
  desc         String?   @db.Text
  count        Int       @default(0) @db.UnsignedInt
  status       Boolean?  @default(true)
  cover_id     Int       @default(0) @db.UnsignedInt
  costs        Int       @default(0) @db.UnsignedInt
  pay_type     Boolean?  @default(true)
  free_count   Int       @default(1) @db.UnsignedTinyInt
  is_hot       Boolean?  @default(false)
  sort         Int       @default(0)
  total_nums   Int?      @default(0) @db.UnsignedSmallInt
  updated_nums Int?      @default(0) @db.UnsignedSmallInt
  created_at   DateTime? @default(now()) @db.Timestamp(0)
  updated_at   DateTime? @default(now()) @db.Timestamp(0)
  deleted_at   DateTime? @db.Timestamp(0)

  @@index([cover_id], map: "idx_cover_id")
  @@index([series_id], map: "idx_series_id")
  @@fulltext([title], map: "idx_title")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_videos {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  group_id   Int       @db.UnsignedInt
  file_id    Int       @default(0)
  title      String    @default("") @db.VarChar(255)
  cover      String?   @default("") @db.VarChar(255)
  desc       String    @default("") @db.VarChar(255)
  sort       Int       @default(0)
  created_at DateTime? @default(now()) @db.Timestamp(0)
  updated_at DateTime? @default(now()) @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)

  @@index([file_id], map: "idx_file_id")
  @@index([group_id], map: "idx_group_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sp_view_histories {
  id             BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  user_id        BigInt
  video_id       BigInt
  is_finished    Boolean   @default(false)
  video_group_id BigInt
  created_at     DateTime  @default(now()) @db.Timestamp(0)
  updated_at     DateTime? @default(now()) @db.Timestamp(0)
  deleted_at     DateTime? @db.Timestamp(0)

  @@index([is_finished], map: "idx_finished")
  @@index([video_group_id, user_id, video_id], map: "idx_vgid_uid")
}
